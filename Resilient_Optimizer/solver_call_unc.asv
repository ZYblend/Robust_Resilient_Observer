function [z] = solver_call_unc(y,u,mu_k,Sigma_inv_k,z_0,H,Phi,r_tau)
%% Function description comments

% Yu Zheng, 2022/09/27

mT = length(y); % length of measured outputs over the entire receding horizon

A_in = Phi -eye(mT);
 
b_in = y-H*u;
 
z = quadprog(A_in.'*A_in, -2*A_in.'*b_in);
 

 
end